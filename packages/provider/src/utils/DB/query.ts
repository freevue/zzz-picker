enum QUERY {
  ENGINES = `
    id,
    nameKo: name_ko,
    isPickup: is_pickup,
    exclusiveAgentId: exclusive_agent_id,
    rank,
    imageUrl: image_url,
    iconUrl: icon_url
  `,
}

export default QUERY
