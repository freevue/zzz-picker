// import { supabase } from '@zzz-picker/supabase'
import { useEffect } from 'react'

// console.log(supabase)

const Chat: React.FC = () => {
  useEffect(() => {}, [])

  return <div className="size-full snap-y overflow-auto scrollbar-hidden">Chat</div>
}

export default Chat
